name: Release

on:
  push:
    tags:
      - 'v*'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          sbom: false

  create-release:
    runs-on: ubuntu-latest
    needs: build-and-push
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build binaries
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          LDFLAGS="-s -w -X main.version=${VERSION}"

          GOOS=linux GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o weather-exporter-linux-amd64 ./cmd/weather-exporter
          GOOS=linux GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o weather-exporter-linux-arm64 ./cmd/weather-exporter
          GOOS=darwin GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o weather-exporter-darwin-amd64 ./cmd/weather-exporter
          GOOS=darwin GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o weather-exporter-darwin-arm64 ./cmd/weather-exporter
          GOOS=windows GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o weather-exporter-windows-amd64.exe ./cmd/weather-exporter

          sha256sum weather-exporter-* > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            weather-exporter-*
            checksums.txt
          generate_release_notes: true
          draft: false
          prerelease: false
